---
import Section from '~/components/Section.astro';

interface Props {
  title?: string;
}

const { title = 'Questions fréquentes' } = Astro.props;
---

<Section title={title}>
  <div class="max-w-3xl divide-y divide-slate-200 border-y border-slate-200">
    <slot />
  </div>
</Section>
